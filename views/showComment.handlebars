 {{> post-details}}
 {{#each (reverse comments) as |comment|}}
    <div>
        <p>{{comment.content}}</p>
        <div>
            <p>{{comment.user.username}}</p>
            <p>{{comment.date_created}}</p>
        </div>
    </div>
    {{/each}}

